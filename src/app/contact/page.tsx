import { Hero } from "@/components/Hero";

export default function Contact() {
  return (
    <div>
      <Hero
        page="contact"
        title="Contact Us"
        pra="Let’s build something that lasts."
      />
    </div>
  );
}
